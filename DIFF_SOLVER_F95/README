###########################################################################
===========================================================================

1D and 2D test programs to solve the heat equation
with Dirichlet boundary conditions and initial condition 

1D:

u_0(x,0) = sin(pi*x).

2D:

u_0(x,y,0) = sin(pi*x)sin(pi*y).

To run:

make clean
make

./test1D

or 

./test2D

This generates the exact solution, FTCS solution, BTCS solution
and CN solution. The 2 norm of the error is then computed for each and wall clock time computed.

TODO:

Usen openmp and MPI on 2D code.

============================================================================
############################################################################

TO DO:

 - Finish parallel version using MPI (tick, issues with error.f90 but that's fine)
 - Add in Jacobi preconditioner for serial code (tick, reduces by at most 10% in the iterations :| )
 - Add in Jacobi preconditioner for parallel code (issues with this, use the stuff from the serial code which works) 
 
 
